# neap-book-crud
## API boiler plate for a book CRUD app


## Project set up

Run the following commands to setup project.

`createdb bookCatalog`

`npm install`

`knex migrate:latest`

`knex seed:run`
